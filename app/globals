@tailwind base;
@tailwind components;
@tailwind utilities;

html,
body {
  min-height: 100%;
}

body {
  background: linear-gradient(180deg, #eef8ff 0%, #fff9ec 100%);
}

::selection {
  background: #ffb703;
  color: #023047;
}

/* Grand mode "vidéoprojecteur" : texte très lisible depuis le fond de la classe */
.projector-text {
  font-size: clamp(1.75rem, 4vw, 3rem);
  line-height: 1.5;
}

.card-shadow {
  box-shadow: 0 10px 30px -10px rgba(2, 48, 71, 0.25);
}

@keyframes confetti-fall {
  0% {
    transform: translateY(-10vh) rotate(0deg);
    opacity: 1;
  }
  100% {
    transform: translateY(110vh) rotate(720deg);
    opacity: 0;
  }
}
